\appendix
\startappendixpages

\chapter{Installation Guide}

The minimum environment is Node.js/npm, PostgreSQL and Expo Go or a browser. Redis 7 is recommended for the worker. Without Redis, the API may use an in-memory KV fallback during development, but queue and retry behaviour is not demonstrated.

\section{Backend Setup}

From the project root, copy the sample configuration, supply PostgreSQL settings and retain \code{AI_PROVIDER=local} when no Gemini key is used. Never commit secrets or service-account files.

\begin{verbatim}
cd demo/backend
cp .env.example .env
npm install
npm run migrate
npm start
\end{verbatim}

The API defaults to \url{http://127.0.0.1:3000}. Use that URL for a basic availability check. Recreate schema and seed data only on a development/test database:

\begin{verbatim}
npm run migrate:fresh
\end{verbatim}

\section{Redis and Worker}

From \code{demo}, start Redis with Docker Compose and open another terminal for the worker:

\begin{verbatim}
docker compose up -d redis
cd backend
npm run worker
\end{verbatim}

Verify \code{REDIS_URL}, \code{JOBS_ENABLED} and timezone in \code{backend/.env}. \code{docker compose down} stops Redis while retaining its volume.

\section{Frontend Setup}

\begin{verbatim}
cd demo/frontend
npm install
EXPO_PUBLIC_API_URL=http://127.0.0.1:3000 npm start
\end{verbatim}

For a phone on the same LAN, replace \code{127.0.0.1} with the backend host's LAN address and use \code{npm run start:lan}. If the device cannot reach that address, use Expo tunnel plus a separate backend tunnel. Never expose AI secrets through frontend environment variables.

\chapter{Quick User Guide}

\begin{enumerate}
  \item Open the application and check that Dashboard shows balance, total income/expense and recent transactions.
  \item Add a transaction from Transactions, or open Chat and enter a sentence such as ``ăn phở 50k sáng nay''.
  \item For image/audio, review and edit the OCR text or transcript. Media results are not stored automatically.
  \item Review amount, type, date, category and wallet in the preview; edit, cancel or confirm. Only confirmation creates a transaction.
  \item Use Budget for limits, Reports for trends/insights and Goals for plans and what-if scenarios.
  \item Correct an incorrect category. Confirmed corrections may be retrieved for a later input.
\end{enumerate}

The prototype has no login and uses \code{default_user}. Do not enter sensitive data or treat results as guaranteed investment advice.

\chapter{Reproduction Commands}

Run each command from the named component directory. Record commit, Node version, provider configuration and PostgreSQL/Redis state without storing secrets in artifacts.

\begin{verbatim}
cd demo/backend
npm test
npm run test:ai
npm run smoke:full
node --test tests/experiment-metrics.test.js
node tests/experiments/classification-benchmark.js --out ../../log
node tests/experiments/feedback-before-after.js --out ../../log

cd ../frontend
npm run ui:smoke
npx expo export --platform web --output-dir /tmp/perfin-web
\end{verbatim}

In the Node 24 artifact environment, \code{npm test} aggregates results by test file; 44/44 does not mean 44 or 182 individual test cases. The final test summary is stored at \path{log/backend-test-run_2026-08-02.json}. The final two runners are offline. Do not rerun the Gemini ablation without deliberately supplying provider access/quota; its historical measurement and reanalysis are under \code{log/}. Media smoke tests require a real provider and fixtures. Without ground truth, report availability pass/fail only. Integration tests need a separate test database and cleanup after each group.

\chapter{Diagram Sources}

The report keeps 14 editable Draw.io sources and one overall use case. Every diagram label is English so the Vietnamese and English reports share the same set. Sources are in \path{latex/figures/drawio/}; PDF/PNG/SVG exports are in \path{latex/figures/rendered/}.

\begin{longtable}{@{}C{1.0cm}L{5.2cm}L{6.8cm}@{}}
  \toprule
  \textbf{No.} & \textbf{Subject} & \textbf{Basename} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{No.} & \textbf{Subject} & \textbf{Basename} \\
  \midrule
  \endhead
  1 & System context & \path{01-system-context} \\
  2 & Runtime architecture & \path{02-runtime-architecture} \\
  3 & Prototype deployment & \path{03-deployment} \\
  4 & Domain class diagram & \path{04-domain-class} \\
  5 & Physical ERD & \path{05-physical-erd} \\
  6 & LLM boundary & \path{06-llm-boundary} \\
  7 & Conversation state & \path{07-conversation-state} \\
  8 & Text transaction sequence & \path{08-text-sequence} \\
  9 & Multimodal input & \path{09-multimodal-flow} \\
  10 & Feedback and classification & \path{10-feedback-flow} \\
  11 & Grounded insight & \path{11-insight-sequence} \\
  12 & Goal planner and what-if & \path{12-goal-flow} \\
  13 & Background job & \path{13-worker-sequence} \\
  14 & Overall use case & \path{14-usecase-overview} \\
  \bottomrule
\end{longtable}
