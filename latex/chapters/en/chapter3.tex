\chapter{Application Results}
\label{chap:results}

\section{Software Requirements Specification}

\subsection{Overall Description}

PERFIN is a mobile prototype for entering, normalizing, managing and analysing personal-finance data. Chat and media complement forms as input channels. The backend performs every financial calculation, constraint check and data change; LLM, OCR and STT services have no direct PostgreSQL access and are not authoritative numeric sources. FR/NFR identifiers and specifications follow the requirements-engineering guidance of ISO/IEC/IEEE~29148:2018 \cite{iso29148}.

\begin{table}[H]
  \centering
  \caption{Product context and constraints}
  \label{tab:product-overview}
  \begin{tabularx}{\textwidth}{@{}L{3.5cm}Y@{}}
    \toprule
    \textbf{Item} & \textbf{Description} \\
    \midrule
    User & A general user records and interprets personal-finance data without requiring statistical expertise. \\
    Environment & React Native/Expo on mobile or web, Express API and PostgreSQL; Redis and a worker are configuration-dependent. \\
    System of record & PostgreSQL stores business data; Redis stores only TTL-bound pending, clarification, cache and queue state. \\
    Constraints & The demo uses \code{default_user}, has no production authentication/authorization, defaults to VND and has no foreign-exchange conversion. \\
    Dependencies & Gemini is an external language provider; PaddleOCR and PhoWhisper run locally on the backend host. Local-media failure returns a real error, never fabricated data. \\
    Assumption & The user reviews previews; guidance does not replace professional financial advice. \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Actors and concerns}
  \label{tab:actors}
  \begin{tabularx}{\textwidth}{@{}L{3.2cm}Y Y@{}}
    \toprule
    \textbf{Actor} & \textbf{Role} & \textbf{Concern or limitation} \\
    \midrule
    User & Enters, edits, confirms, manages and views reports. & Language/media-derived data must be reviewed before storage. \\
    External/local AI adapters & Gemini returns a typed draft or narration; local media runtimes return OCR text or a transcript. & No adapter executes business operations or writes the database; extracted text is still untrusted. \\
    Background worker & Runs reminders, analysis and file cleanup. & Jobs require user scope, retry and duplicate-effect protection. \\
    Developer administrator & Configures environment, migrations and providers. & Not a business actor of the application. \\
    \bottomrule
  \end{tabularx}
\end{table}

Shared rules are: business amounts must be finite and valid; transaction dates cannot be in the future; wallets and categories belong to the current profile; negative wallet balances are allowed; chat/media operations require preview and confirmation; multi-record updates are atomic; reports exclude soft-deleted transactions by default; and insufficient data yields a warning or \code{null}, never an invented value.

\widereportfigure[\subsection{Functional Requirements}][Figure~\ref{fig:usecase-overview} contains observable actor goals only. Parser, validation, transaction, TTL and retry behaviour belongs in the flow tables or the sequence/flow diagrams in Section~\ref{subsec:detailed-design}.]{figures/drawio/14-usecase-overview.png}{Overall PERFIN use case diagram. Feature details are specified with flow tables instead of twelve separate diagrams.}{fig:usecase-overview}

\begin{longtable}{@{}L{1.5cm}L{5.0cm}L{3.0cm}L{4.5cm}@{}}
  \caption{Functional-requirement catalogue and location of the full specification}
  \label{tab:fr-catalogue}\\
  \toprule
  \textbf{ID} & \textbf{Capability} & \textbf{Priority} & \textbf{Full specification} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{ID} & \textbf{Capability} & \textbf{Priority} & \textbf{Full specification} \\
  \midrule
  \endhead
  FR-01 & Natural-language transaction entry & Mandatory & Chapter 3, Table~\ref{tab:fr01} \\
  FR-02 & Image and speech transaction entry & Desirable & Chapter 3, Table~\ref{tab:fr02} \\
  FR-03 & Clarification and confirmation & Mandatory & Chapter 3, Table~\ref{tab:fr03} \\
  FR-04 & Classification and correction retrieval & Mandatory & Chapter 3, Table~\ref{tab:fr04} \\
  FR-05 & Financial-data management & Mandatory & Chapter 3, Table~\ref{tab:fr05} \\
  FR-06 & Wallet transfer and special cash flows & Desirable & Appendix, Table~\ref{tab:app-fr06} \\
  FR-07 & Deterministic financial analytics & Mandatory & Chapter 3, Table~\ref{tab:fr07} \\
  FR-08 & Grounded insights and narration & Desirable & Chapter 3, Table~\ref{tab:fr08} \\
  FR-09 & Budget management and forecasting & Mandatory & Chapter 3, Table~\ref{tab:fr09} \\
  FR-10 & Goal planning and what-if scenarios & Mandatory & Chapter 3, Table~\ref{tab:fr10} \\
  FR-11 & Recurring bills and proactive jobs & Desirable & Appendix, Table~\ref{tab:app-fr11} \\
  FR-12 & Export and expired-file cleanup & Optional & Appendix, Table~\ref{tab:app-fr12} \\
  \bottomrule
\end{longtable}

\begin{frtable}{FR-01 --- Enter Transactions from Text}{tab:fr01}
  \textbf{Feature name} & Enter transactions from natural-language text. \frtablerow
  \textbf{ID} & FR-01. \frtablerow
  \textbf{User} & User. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The user needs fast entry; the AI Orchestrator returns a typed draft; Transaction Service protects the ledger. \frtablerow
  \textbf{Summary} & Convert one sentence into one or more structured transactions and create a preview before writing. \frtablerow
  \textbf{Relationships} & Includes FR-03 and FR-04; uses FR-05 after confirmation. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{User submits text.} \frstep{Step 2:}{Extract and normalize.} \frstep{Step 3:}{Validate schema, category and wallet.} \frsubstep{Sub 1:}{Build preview.} \frstep{Step 4:}{User confirms.} \frstep{Step 5:}{Commit atomically and return the result.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Preview:}{Store a TTL-bound draft.} \frsubstep{Sub 1.1:}{Show amount, type, date, category, wallet and warnings.} \frtablerow
  \textbf{Alternate flows} & \frstep{Step 2:}{Provider failure uses the local parser or returns an error.} \frstep{Step 3:}{Missing/ambiguous fields invoke FR-03.} \frstep{Step 4:}{Edit or cancel causes no write.} \frtablerow
\end{frtable}

\begin{frtable}{FR-02 --- Enter Transactions from Image or Speech}{tab:fr02}
  \textbf{Feature name} & Enter a transaction from a receipt image or speech. \frtablerow
  \textbf{ID} & FR-02. \frtablerow
  \textbf{User} & User; local media runtime; optional external language adapter. \frtablerow
  \textbf{Necessity} & Desirable. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The user needs visible raw text; local OCR/STT reports provider and failure; the backend rejects invalid files. \frtablerow
  \textbf{Summary} & Convert image/audio into reviewable text, then reuse FR-01. \frtablerow
  \textbf{Relationships} & Includes FR-01 and FR-03. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Select a valid file.} \frstep{Step 2:}{Backend invokes OCR or STT.} \frstep{Step 3:}{Display raw text and provider.} \frsubstep{Sub 1:}{User edits/confirms text.} \frstep{Step 4:}{Continue with FR-01.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Media review:}{Edit a voice transcript.} \frsubstep{Sub 1.1:}{For a receipt, select the total or individual items when present.} \frtablerow
  \textbf{Alternate flows} & \frstep{Step 1:}{An invalid type or size above 10 MB is rejected.} \frstep{Step 2:}{A provider that returns no text produces an error and no mock or pending draft.} \frtablerow
\end{frtable}

\begin{frtable}{FR-03 --- Clarify and Confirm Pending Transactions}{tab:fr03}
  \textbf{Feature name} & Clarification, preview and pending-transaction confirmation. \frtablerow
  \textbf{ID} & FR-03. \frtablerow
  \textbf{User} & User. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The user can edit/cancel; the KV store applies TTL; a preview can be consumed once. \frtablerow
  \textbf{Summary} & Collect missing fields across turns and prevent inferred data from being written. \frtablerow
  \textbf{Relationships} & Used by FR-01, FR-02, FR-09, FR-10 and FR-12. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Store intent and missing fields.} \frstep{Step 2:}{Ask for the next field.} \frstep{Step 3:}{Merge and revalidate.} \frsubstep{Sub 1:}{Build or edit a preview.} \frstep{Step 4:}{Atomically claim pending state.} \frstep{Step 5:}{Call the business service.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Preview:}{Attach a \code{pending_id} with a five-minute TTL.} \frsubstep{Sub 1.1:}{Update only with a valid payload.} \frtablerow
  \textbf{Alternate flows} & \frstep{Step 3:}{Continue asking while fields are missing.} \frstep{Step 4:}{Expired, incorrect or claimed IDs cannot write.} \frstep{Cancel:}{Clear state and perform no write.} \frtablerow
\end{frtable}

\begin{frtable}{FR-04 --- Classify and Learn from Corrections}{tab:fr04}
  \textbf{Feature name} & Category classification and correction retrieval. \frtablerow
  \textbf{ID} & FR-04. \frtablerow
  \textbf{User} & User. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The matcher must be explainable; the user corrects labels; feedback failure must not corrupt a committed transaction. \frtablerow
  \textbf{Summary} & Select a category by exact/alias/fuzzy/LLM methods and reuse confirmed corrections. \frtablerow
  \textbf{Relationships} & Extends FR-01 and may be triggered by an FR-05 category edit. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Normalize the description.} \frstep{Step 2:}{Load same-profile categories and corrections.} \frstep{Step 3:}{Rank candidates.} \frsubstep{Sub 1:}{Apply a strong correction.} \frstep{Step 4:}{Return category, confidence and match kind.} \frstep{Step 5:}{Store feedback after confirmation.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Correction:}{Select at most five similar corrections.} \frsubstep{Sub 1.1:}{Use them as context or a controlled override.} \frtablerow
  \textbf{Alternate flows} & \frstep{Step 3:}{A low score, small margin or conflicting corrections return Other or request a selection.} \frstep{Feedback write:}{A failure is logged only.} \frtablerow
\end{frtable}

\begin{frtable}{FR-05 --- Manage Financial Data}{tab:fr05}
  \textbf{Feature name} & Manage transactions, wallets and categories. \frtablerow
  \textbf{ID} & FR-05. \frtablerow
  \textbf{User} & User. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The user needs CRUD and restore; services maintain balance and history consistency. \frtablerow
  \textbf{Summary} & Create, view, edit, soft-delete and restore transactions and manage profile-scoped categories/wallets. \frtablerow
  \textbf{Relationships} & Used by FR-01/FR-02 and supplies FR-07--FR-12. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Select an operation.} \frstep{Step 2:}{Validate payload and ownership.} \frstep{Step 3:}{Compute the balance delta.} \frsubstep{Sub 1:}{Update the transaction and wallet atomically.} \frstep{Step 4:}{Return the record and new balance.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Update:}{Write the transaction.} \frsubstep{Sub 1.1:}{Update the wallet.} \frsubstep{Sub 1.2:}{Commit, then invalidate cache best-effort.} \frtablerow
  \textbf{Alternate flows} & \frstep{Step 2:}{Out-of-scope IDs or invalid payloads are rejected.} \frstep{Step 3:}{A negative balance remains valid.} \frstep{Before commit:}{Any failure rolls back.} \frtablerow
\end{frtable}

\begin{frtable}{FR-07 --- Analyse Financial Data}{tab:fr07}
  \textbf{Feature name} & Trend, anomaly, runway, recurring and correlation analysis. \frtablerow
  \textbf{ID} & FR-07. \frtablerow
  \textbf{User} & User; Background worker. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & Analytics needs clean data; users need period, sample size and method. \frtablerow
  \textbf{Summary} & Compute deterministic facts over non-deleted data; trend/correlation use completed periods and fixed calendar axes are zero-filled. \frtablerow
  \textbf{Relationships} & Reads FR-05 and supplies FR-08 and FR-11. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Select the analysis and period.} \frstep{Step 2:}{Query profile data.} \frstep{Step 3:}{Normalize the calendar axis and check sample count.} \frsubstep{Sub 1:}{Run the algorithm.} \frstep{Step 4:}{Return facts with window, method, parameter and warning metadata.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Analytics:}{Run OLS, z-score/IQR, runway, recurring or Pearson through one result contract.} \frtablerow
  \textbf{Alternate flows} & \frstep{Sparse data or zero variance:}{Return a degraded result or \code{null}.} \frstep{Limit:}{Make no causal claim and fabricate no transaction.} \frtablerow
\end{frtable}

\begin{frtable}{FR-08 --- Generate Grounded Insights}{tab:fr08}
  \textbf{Feature name} & Grounded insight and persona narration. \frtablerow
  \textbf{ID} & FR-08. \frtablerow
  \textbf{User} & User; External AI service. \frtablerow
  \textbf{Necessity} & Desirable. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The user needs clear explanation; Analytics owns facts; the narrator cannot alter numbers. \frtablerow
  \textbf{Summary} & Explain precomputed facts and return them with the narration for traceability. \frtablerow
  \textbf{Relationships} & Includes FR-07 and may use a persona when consent is enabled. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Receive a question or report request.} \frstep{Step 2:}{Generate facts.} \frstep{Step 3:}{Load a permitted persona.} \frsubstep{Sub 1:}{Narrate the facts.} \frstep{Step 4:}{Return narration, facts and provider metadata.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Narration:}{Lock numbers and units in the prompt.} \frsubstep{Sub 1.1:}{Use a deterministic template when the provider fails.} \frtablerow
  \textbf{Alternate flows} & \frstep{Sparse facts:}{Return a warning.} \frstep{Limit:}{A complete numeric-grounding checker remains a design target and is not presented as measured.} \frtablerow
\end{frtable}

\begin{frtable}{FR-09 --- Manage and Forecast Budgets}{tab:fr09}
  \textbf{Feature name} & Monthly budgets, recommendations and forecasts. \frtablerow
  \textbf{ID} & FR-09. \frtablerow
  \textbf{User} & User. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Medium. \frtablerow
  \textbf{Participants and concerns} & Users need progress visibility; Budget Engine needs sufficient history and must not apply limits automatically. \frtablerow
  \textbf{Summary} & Compute spent/remaining, forecast month end and propose limits from history or a ratio framework. \frtablerow
  \textbf{Relationships} & Reads FR-05 and may invoke FR-03 from chat. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Select a month.} \frstep{Step 2:}{Query spending.} \frstep{Step 3:}{Compute progress and forecast.} \frsubstep{Sub 1:}{Produce a warning or recommendation.} \frstep{Step 4:}{Confirm before saving a recommendation.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Recommendation:}{Zero-fill months.} \frsubstep{Sub 1.1:}{Compute limits.} \frsubstep{Sub 1.2:}{Proportionally shrink raw allocations and reconcile rounding when a group exceeds its cap.} \frtablerow
  \textbf{Alternate flows} & \frstep{Fewer than three history months:}{Produce a warning.} \frstep{Invalid data:}{A non-positive amount or non-expense category is rejected.} \frtablerow
\end{frtable}

\begin{frtable}{FR-10 --- Plan Goals and What-if Scenarios}{tab:fr10}
  \textbf{Feature name} & Savings, purchase and debt-payoff goal planning. \frtablerow
  \textbf{ID} & FR-10. \frtablerow
  \textbf{User} & User. \frtablerow
  \textbf{Necessity} & Mandatory. \frtablerow
  \textbf{Classification} & Complex. \frtablerow
  \textbf{Participants and concerns} & The user needs formula-backed progress; Goal Planner must expose infeasible assumptions. \frtablerow
  \textbf{Summary} & Calculate contribution, duration, amortization and what-if plans without modifying the original until confirmation. \frtablerow
  \textbf{Relationships} & Reads FR-05/FR-07 and may use FR-03. \frtablerow
  \textbf{Normal flow} & \frstep{Step 1:}{Enter the goal.} \frstep{Step 2:}{Validate amount, date and rate.} \frstep{Step 3:}{Compute allocatable cash.} \frsubstep{Sub 1:}{Run the goal-specific formula.} \frstep{Step 4:}{Return the plan and warnings.} \frstep{Step 5:}{Save after confirmation.} \frtablerow
  \textbf{Subflows} & \frsubstep{Sub 1 --- Calculation:}{What-if reruns the saving/purchase function with an additional contribution.} \frsubstep{Sub 1.1:}{Debt payoff uses monthly simulation.} \frsubstep{Sub 1.2:}{The original plan remains unchanged and no scenario is persisted.} \frtablerow
  \textbf{Alternate flows} & \frstep{Invalid inputs:}{Request correction.} \frstep{Infeasible payment or horizon:}{Payment less than or equal to first-month interest warns of negative amortization; zero payment and a 600-month horizon are reported explicitly.} \frstep{Cancel:}{Perform no write.} \frtablerow
\end{frtable}

\subsection{Non-functional Requirements}

\begin{longtable}{@{}L{1.3cm}L{3.0cm}L{5.8cm}L{3.8cm}@{}}
  \caption{Non-functional requirements and acceptance methods}\label{tab:nfr}\\
  \toprule
  \textbf{ID} & \textbf{Group} & \textbf{Testable requirement} & \textbf{Measurement} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{ID} & \textbf{Group} & \textbf{Testable requirement} & \textbf{Measurement} \\
  \midrule
  \endhead
  NFR-01 & Recovery & Provider/Redis failure fabricates no data; restore demo service and backup data within three hours after an incident. & Fault injection, restart and restore on a test database. \\
  NFR-02 & Integrity & No partial create, update, delete, restore, transfer or payment; credentials are never transmitted or logged in clear text. & Rollback tests and configuration/log scanning; TLS for network deployment. \\
  NFR-03 & UI & Vietnamese blue--slate interface on neutral surfaces, consistent navigation and no overflow at mobile viewports. & Manually inspect the main screens at a mobile viewport for navigation, content and layout overflow. \\
  NFR-04 & Timing & Acknowledge within three seconds; target text p95 $\leq3$ s and media p95 $\leq8$ s in a disclosed environment. & At least 30 runs per flow with provider, hardware, cache and error rate. \\
  NFR-05 & Correctness & Algorithms match expected normal/boundary cases; negative balances are valid; narrator invents no number. & Unit tests, invariants and facts--response checker. \\
  NFR-06 & Privacy & Remove temporary media; use traits only with consent; ID queries include user scope. & Temporary-file, consent and cross-scope tests. \\
  NFR-07 & Worker consistency & Retrying an event/job creates no duplicate payment, message or export. & Repeat fingerprints and count effects. \\
  NFR-08 & Traceability & Analysis includes schema version, timestamp, component value and metadata for unit, window, sample count, method, parameters, warnings and degradation status. & Unit/contract tests and inspection of returned metadata. \\
  \bottomrule
\end{longtable}

Recovery, latency, OCR/STT accuracy and complete numeric grounding remain acceptance targets; unmeasured targets are not presented as achieved results.

\section{Software Design}

\subsection{Architecture}

PERFIN is a modular monolith: domain services are separated by responsibility while the API remains one process; a separate worker handles background jobs. This matches project scale, reduces operating cost and preserves independent module tests.

\widereportfigure{figures/drawio/02-runtime-architecture.png}{PERFIN runtime architecture; the deterministic core creates and stores facts while the AI Orchestrator coordinates semantics.}{fig:runtime-architecture}

\begin{table}[H]
  \centering
  \caption{Architectural components and responsibilities}
  \label{tab:components}
  \begin{tabularx}{\textwidth}{@{}L{3.2cm}Y Y@{}}
    \toprule
    \textbf{Component} & \textbf{Responsible for} & \textbf{Not responsible for} \\
    \midrule
    Mobile App & Capture input, show previews/facts and confirm. & Database access or AI secrets. \\
    Express Routes & HTTP contracts, upload and input validation. & Analytical formulas. \\
    Core Services & Business rules, transactions and idempotency. & LLM-generated numbers. \\
    Analytics, Budget, Goal & Deterministic facts and plans. & Arbitrary advice. \\
    AI Orchestrator & Tool routing, extraction, narration and fallback. & Direct database writes. \\
    PostgreSQL & System of record, constraints and aggregates. & Temporary conversation state. \\
    Redis/BullMQ & TTL state, cache, queue and retry. & Financial source of truth. \\
    \bottomrule
  \end{tabularx}
\end{table}

Microservices were considered but not selected because prototype scale does not justify deployment, tracing and distributed-consistency costs. Serverless is also a poor fit for a long-running worker and local media providers. A modular monolith keeps database transactions simple and leaves a future extraction path when real load evidence exists.

\subsection{Data Design}

The data design translates the information domain into typed objects and storage responsibilities. The mapping is derived from migrations \code{001}--\code{009}, \path{perfin_schema.sql} and \path{perfin_schema.dbml}; the migrations remain the upgrade source of truth. The end-to-end mapping flow is

\[
  \text{input} \;\rightarrow\; \text{validation/service}
  \;\rightarrow\; \text{domain object}
  \;\rightarrow\; \text{PostgreSQL}.
\]

Text, voice, image and form input first become a parsed draft. The validation and service layer checks shape, user scope, domain constraints and the confirmation boundary before constructing a domain object. Only the service writes the authoritative financial record. Analytics queries those PostgreSQL records and computes deterministic facts before any narrator receives them. Pending clarification and confirmation claims, cache entries, rate-limit counters, queue jobs and retry metadata are operational state: Redis/BullMQ carries them with a TTL, with the supported in-memory fallback used when Redis is unavailable. Uploaded media, generated exports and backup artifacts use a bounded filesystem lifecycle and are cleaned up or expired according to their purpose.

The UML Class Diagram provides the domain-object view of this translation: typed attributes, visibility, package boundaries and principal associations or compositions. It is the single data-design diagram in this report; SQL columns, keys and operational tables remain documented by the schema sources and the accompanying data tables below.

\widereportfigure{figures/drawio/04-domain-class.png}{PERFIN UML entity model for the twelve core classes, grouped by identity and personalization, ledger, planning and recurring, and conversation and feedback responsibilities.}{fig:domain-class}

\begin{table}[H]
  \centering
  \caption{Data stores and their responsibilities}
  \label{tab:data-stores}
  \begin{tabularx}{\textwidth}{@{}L{3.0cm}L{3.7cm}Y@{}}
    \toprule
    \textbf{Store} & \textbf{Contents} & \textbf{Responsibility and lifecycle} \\
    \midrule
    PostgreSQL & Core entities, supporting records, constraints and indexes. & Persistent and authoritative system of record for financial, profile and history data. Services enforce user scope, transactions, foreign keys, checks and soft-delete filtering. \\
    Redis/BullMQ & Pending clarification and confirmation state, cache, rate limits, queue jobs and retry metadata. & TTL-bound operational state only; Redis is never the financial source of truth. When unavailable, supported flows use the in-memory fallback and background jobs degrade safely. \\
    Filesystem & Temporary media uploads, generated exports and backup artifacts. & Bounded-lifecycle files. Media is deleted after processing; export and backup cleanup removes expired files according to retention policy. PostgreSQL keeps the relevant history metadata. \\
    \bottomrule
  \end{tabularx}
\end{table}

\small
\begin{longtable}{@{}L{3.0cm}L{3.0cm}L{3.8cm}L{4.2cm}@{}}
  \caption{Core entities in alphabetical order}\label{tab:data-entities}\\
  \toprule
  \textbf{Entity} & \textbf{Domain type} & \textbf{PostgreSQL table} & \textbf{Responsibility} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Entity} & \textbf{Domain type} & \textbf{PostgreSQL table} & \textbf{Responsibility} \\
  \midrule
  \endhead
  \code{AIFeedback} & Feedback record & \code{ai_feedback_logs} & Stores the original AI result, user correction, feedback type and optional transaction link. \\
  \code{AIPersonality} & Personalization catalogue & \code{ai_personalities} & Stores selectable persona metadata and the style prompt used for conversation. \\
  \code{Budget} & Planning entity & \code{budgets} & Stores a positive category limit for a calendar month and year. \\
  \code{Category} & Ledger catalogue & \code{categories} & Stores the income/expense taxonomy, optional parent category and user scope. \\
  \code{ChatMessage} & Conversation record & \code{chat_messages} & Stores user, assistant or system messages, JSONB metadata and optional personality context. \\
  \code{FinancialGoal} & Planning entity & \code{financial_goals} & Stores a saving, debt-payoff or purchase target, progress, deadline and optional wallet link. \\
  \code{RecurringBill} & Recurring plan & \code{recurring_bills} & Stores recurring amount, frequency, due date, status and optional category/wallet links. \\
  \makecell[l]{\code{Recurring}\\\code{Payment}} & Recurring ledger record & \makecell[l]{\code{recurring_bill_}\\\code{payments}} & Stores one bill period, payment status, amount and optional settlement transaction. \\
  \code{Transaction} & Ledger record & \code{transactions} & Stores a positive amount, transaction type and source metadata, with a soft-delete state. \\
  \code{User} & Ownership root & \code{users} & Stores the profile, data-scope key, payday, language and personalization consent. \\
  \code{UserTrait} & Personalization record & \code{user_traits} & Stores consent-dependent traits used to personalize responses. \\
  \code{Wallet} & Ledger account & \code{wallets} & Stores a named wallet, type, currency, current balance and initial balance. \\
  \bottomrule
\end{longtable}
\normalsize

Six supporting PostgreSQL tables are intentionally outside the two core diagrams because they are operational or historical records rather than core domain entities: \code{backup_config} stores per-user backup policy; \code{budget_history} audits budget changes; \code{export_history} tracks generated files and expiry; \code{investment_pnl} records investment profit/loss; \code{recurring_suggestions_dismissed} prevents repeated dismissed suggestions; and \code{wallet_transfers} records wallet-to-wallet movement. They remain user-scoped where applicable and are accessed through their owning service.

The relational rules use \code{DECIMAL(15,2)} for monetary values, SQL enums and check constraints for constrained states, dates for calendar events, timestamps for audit metadata and \code{JSONB} for metadata and provider/AI payloads. Report queries exclude transactions with a non-null \code{deleted_at}; referenced transaction categories use restricted deletion or controlled re-tagging; nullable links are set to null according to the schema; and user-scoped queries include the \code{user_id}/\code{user_key} scope before returning an identifier. Internal transfers require wallets with matching currencies, while runway includes only liquid VND \code{cash}, \code{bank} and \code{e\_wallet} balances.

The service layer claims pending state atomically and lets Redis state expire; a Redis key is never treated as authoritative financial data. LLM, OCR and STT adapters can propose text or typed drafts, but they cannot write authoritative facts. Temporary media must be removed after processing, and export or backup files must be cleaned up when their retention or expiry policy is reached.

\subsection{Detailed Design}
\label{subsec:detailed-design}

\widereportfigure[\subsubsection{LLM Boundary and Input}][The LLM only creates a typed command or narrates facts. The backend validates schema and domain rules, builds a preview and waits for confirmation before opening a transaction. For analysis, SQL and the engine create facts before narration. A parser or deterministic template provides a testable fallback.]{figures/drawio/06-llm-boundary.png}{LLM responsibility boundary: typed drafts and narration remain outside validation, calculation and data-write authority.}{fig:llm-boundary}

\widereportfigure[][Parsing/preview has no side effect; confirm/commit atomically claims pending state. State contains intent, missing fields, candidates and a draft with a five-minute TTL. Concurrent confirmation must not create two transactions from one \code{pending_id}.]{figures/drawio/08-text-sequence.png}{Text-entry sequence; the data-write transaction starts only after confirmation.}{fig:text-sequence}

\widereportfigure[][OCR/STT returns only raw text and provider metadata. Current ground truth is too small and receipt line totals are not automatically reconciled, so Figure~\ref{fig:multimodal-flow} is a design flow, not evidence of accuracy.]{figures/drawio/09-multimodal-flow.png}{Image and speech processing; both branches converge on the text pipeline after raw-text review.}{fig:multimodal-flow}

\widereportfigure[\subsubsection{Classification and Correction Retrieval}\label{subsec:classification-design}]{figures/drawio/10-feedback-flow.png}{Classification and correction-retrieval flow; feedback is stored only after confirmation.}{fig:feedback-flow}

The matcher uses

\begin{equation}
  s=\max(s_{edit},\;0.92s_{dice},\;s_{contain}).
\end{equation}

Let $U,V$ be normalized token sets and choose $|U|\leq|V|$. The implementation defines containment as

\begin{equation}
  s_{contain}=\begin{cases}
    \min\!\left(0.94,\;0.82+0.12\dfrac{|U|}{|V|}\right),
      & U\subseteq V\ \text{and}\ |U|\geq2,\\
    0, & \text{otherwise}.
  \end{cases}
\end{equation}

Selection proceeds through exact name, exact alias and fuzzy matching. Input length is measured after normalization: input longer than four characters uses $\tau=0.82$, shorter input uses $\tau=0.90$, and the best candidate must exceed the second by $m=0.08$. These are prototype starting values, not universal optima. Retrieval loads at most 200 recent corrections and overrides only with a record of the same income/expense type; legacy records without type are ineligible. A fuzzy correction needs at least 0.88 similarity, and competing labels require at least 0.8 agreement for the winner. Multiple labels for one exact text return fallback/clarification instead of majority voting.

\subsubsection{Analytics and Planning Algorithms}
\label{subsec:analytics-design}

Default windows are 14 calendar days for runway, 30 calendar days for anomaly, 200 days for recurring discovery, six completed months for trend, 60 calendar days for day-of-week analysis and 12 completed ISO weeks for correlation. Fixed day/month/week axes zero-fill inactive periods; an incomplete current month/week is excluded from comparisons. A payday is a local day-of-month and day 31 is clamped to the target month's last calendar day.

Runway uses only liquid VND balance:

\begin{equation}
  B=\sum_{\substack{w.currency=\mathrm{VND}\\
  w.type\in\{\mathrm{cash},\mathrm{bank},\mathrm{e\_wallet}\}}} balance_w.
\end{equation}

For daily expense $e_j$ and $W=14$,

\begin{equation}
  \bar e_W=\frac{1}{W}\sum_{j=1}^{W}e_j,\qquad
  d=\left\lfloor\frac{B}{\bar e_W}\right\rfloor.
\end{equation}

If $B\leq0$, then $d=0$; if $\bar e_W=0$, the depletion date is undefined. For monthly spending $y_j$, the trend detector keeps the zero-filled six-month axis but requires at least three positive observed months, then uses OLS and emits a signal only when slope $b>0$, $R^2\geq0.5$, and mean stepwise percentage change (over steps with a positive preceding value) is at least 10\%.

Anomaly detection uses the sample standard deviation over all 30 days. It is evaluated only when at least four calendar days have positive spending, and flags only the upper tail when

\begin{equation}
  z_i=\frac{e_i-\bar e}{s}\geq2.5
  \quad\text{or}\quad
  e_i>Q_3+1.5\,IQR.
\end{equation}

When $s=0$, the implementation sets $z_i=0$; when $IQR=0$, the IQR branch is disabled. Thus a constant series cannot become anomalous merely because the denominator is undefined.

Recurring grouping normalizes descriptions and simultaneously requires at least three occurrences, every $\delta_i=|a_i-\bar a|/\bar a\leq0.15$, and gap coefficient of variation $CV_g=\sigma_g/\bar g\leq0.12$ (population $\sigma_g$ over positive gaps). Every gap must fit one cadence: weekly 5--9 days, monthly 26--35 days or quarterly 80--100 days. A candidate is active only when its latest occurrence is no older than that cadence's maximum gap; the output also records \code{lastSeen} and the calendar-clamped \code{nextExpected}. There is no default amount ceiling. The monthly estimate is

\begin{equation}
  \widehat a_{month}=\begin{cases}
    \bar a\,52/12, & \text{weekly},\\
    \bar a, & \text{monthly},\\
    \bar a/3, & \text{quarterly}.
  \end{cases}
\end{equation}

Correlation zero-fills a shared 12-week axis, keeps categories observed in at least four weeks and skips undefined Pearson pairs (fewer than three paired observations or zero variance). It returns only the strongest positive pair when $r\geq0.6$; association does not imply causation. Day-of-week analysis uses the 60-day window, requires at least four observed weekdays and emits a signal only when the peak-to-mean ratio reaches 1.8.

For category spending $s_{c,j}$ across $m$ months, a five-percent-buffer baseline is

\begin{equation}
  b_c=1.05\left(\frac{1}{m}\sum_{j=1}^{m}s_{c,j}\right).
\end{equation}

Month-end forecast is $\widehat S_D=(S/d_e)D$, where $S$ is spending so far, $d_e$ elapsed calendar days including today and $D$ month length. The 50/30/20 and hybrid strategies shrink raw allocations when they exceed a cap; post-rounding reconciliation keeps each group at or below its cap. Needs, wants and savings rates may not sum to more than 1; recommendations require confirmation.

For a saving or purchase goal with remaining amount $R$ and monthly contribution $M>0$, $n_s=\lceil R/M\rceil$. For current debt principal $L$ and nominal annual percentage rate $i$, the implementation uses monthly rate $r=i/(12\times100)$. With a debt deadline of $n_d$ months, the required end-of-month level payment is

\begin{equation}
  P=\frac{Lr}{1-(1+r)^{-n_d}}
\end{equation}

for $r>0$, and $P=L/n_d$ for $r=0$. A payment less than or equal to first-month interest triggers a negative-amortization warning; a zero payment is reported separately. Without a deadline, the implementation simulates month by month up to a 600-month default horizon. What-if currently reruns the saving/purchase function with an additional contribution; debt plans are not changed by that scenario, and no what-if result is persisted.

\widereportfigure{figures/drawio/12-goal-flow.png}{Goal planning and what-if flow; only final confirmation persists a goal.}{fig:goal-flow}

\widereportfigure[\subsubsection{Grounded Insight Generation}][Facts contract version 1.0 returns \code{schema_version}, \code{generated_at}, each component value and \code{metadata.components}. Per-component metadata contains \code{unit}, \code{window}, \code{sample_count}, \code{method}, \code{parameters}, \code{warnings}, and \code{status} in \code{ok/no_signal/degraded}. The window object records \code{size}, \code{unit} and \code{zero_filled}; selected components add \code{category_count} or \code{wallet_count}. A local failure appears in \code{degraded_components} without removing the contract. A persona changes tone only with consent; the response includes narration and facts for UI/test comparison. A numeric checker covering every expression remains a target design.]{figures/drawio/11-insight-sequence.png}{Grounded-insight sequence; Analytics returns facts before persona/LLM narration.}{fig:insight-sequence}

The calculation path, numeric examples and boundary cases for matching, correction, Analytics, Budget, Goal and the facts contract are expanded in Appendix~\ref{app:algorithms}--\ref{app:facts-contract}; this chapter keeps the main formulas and design decisions for a coherent core narrative.

\subsection{UI Design}
\label{subsec:ui-design}

The interface follows a mobile-first design with five primary tabs: Dashboard, Budget, Chat, Report and More; Chat occupies the central position as the preferred input action. Shared design tokens use blue for the brand, slate for text and neutral surfaces for content. Green and red are reserved for the semantic distinction between income and expense. Headers, cards, list rows, buttons and spacing reuse the same tokens, while primary touch targets provide an effective minimum size of 44 pixels.

The images in this subsection were regenerated on 13 August 2026 from the current Expo web snapshot at a $390\times844$ CSS-pixel viewport, device scale factor 2 and synthetic demo data. Chat history is isolated so that the preview state remains legible. The images illustrate design decisions; they are not manual-test results or native-device screenshots.

Figure~\ref{fig:ui-dashboard-chat} shows the Dashboard information hierarchy and the review-before-write interaction in Chat. The Dashboard prioritizes balance, income and expense, and recent transactions. Chat converts natural input into a card containing amount, category and date, followed by explicit confirm, edit and cancel actions. Displaying the card alone does not write to the ledger.

Figure~\ref{fig:ui-budget-report} applies the same card language and semantic colours to two read-oriented flows. Budget organizes period progress, forecast warnings and recommendations into a visible hierarchy. Report separates income and expense totals, deficit, multi-period insight and category breakdown so that readers can move from summary to detail.

Figure~\ref{fig:ui-goal-plan} shows preview-before-save for goals: the backend validates the displayed values before enabling save. What-if guidance leaves the goal unchanged.

\begin{figure}[p]
  \centering
  \begin{minipage}[t]{0.44\textwidth}
    \centering
    \includegraphics[width=\linewidth,height=0.68\textheight,keepaspectratio]{figures/screenshots/01-dashboard.png}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.44\textwidth}
    \centering
    \includegraphics[width=\linewidth,height=0.68\textheight,keepaspectratio]{figures/screenshots/02-chat-preview.png}
  \end{minipage}
  \caption{Dashboard (left) and a Chat transaction preview (right) in the redesigned interface.}
  \label{fig:ui-dashboard-chat}
\end{figure}

\begin{figure}[p]
  \centering
  \begin{minipage}[t]{0.44\textwidth}
    \centering
    \includegraphics[width=\linewidth,height=0.68\textheight,keepaspectratio]{figures/screenshots/03-budget.png}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.44\textwidth}
    \centering
    \includegraphics[width=\linewidth,height=0.68\textheight,keepaspectratio]{figures/screenshots/04-report.png}
  \end{minipage}
  \caption{Budget (left) and Report (right) screens with consistent financial-data presentation.}
  \label{fig:ui-budget-report}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.38\textwidth,height=0.70\textheight,keepaspectratio]{figures/screenshots/05-goal-plan.png}
  \caption{Goal-plan preview in the redesigned interface; the plan has not been saved at capture time.}
  \label{fig:ui-goal-plan}
\end{figure}

\clearpage
\section{Testing}
\label{sec:testing}

\subsection{Test Plan}

Testing is deliberately bounded to the correctness of data handling, algorithms and the prototype's main functional flows. The existing automated suite remains unchanged as a regression safety net, while manual system testing adds the author's interaction perspective. Conclusions apply only to the published cases, fixtures and environment.

\begin{longtable}{@{}L{2.4cm}L{3.5cm}L{4.3cm}L{4.0cm}@{}}
  \caption{Simplified hybrid test strategy}\label{tab:test-strategy}\\
  \toprule
  \textbf{Level} & \textbf{Scope} & \textbf{Method} & \textbf{Scope of conclusion} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Level} & \textbf{Scope} & \textbf{Method} & \textbf{Scope of conclusion} \\
  \midrule
  \endhead
  Automated unit and regression tests & Formulas, invariants, parser and validation. & Run deterministic \code{node:test} cases over controlled fixtures. & Confirms specified cases only; it does not establish out-of-sample accuracy. \\
  Internal integration & Route, service and model layers. & Check contracts, error branches and rollback with mocks or in-memory substitutes. & Confirms in-process coordination; it is not evidence of live PostgreSQL or Redis execution. \\
  Manual system testing & Main functional flows in the interface. & The author uses demo data and compares observations with expected results. & Confirms selected scenarios only; it does not measure usability, OCR/STT accuracy or performance. \\
  \bottomrule
\end{longtable}

\subsection{Results and Analysis}
\label{subsec:results}

The commands in Table~\ref{tab:automated-test-results} were rerun on 14 August 2026 against the current source snapshot using Node.js v24.16.0, with Redis and jobs disabled and the local parser selected. Results are reported in the unit produced by each command.

\begin{table}[H]
  \centering
  \caption{Automated test results}
  \label{tab:automated-test-results}
  \begin{tabularx}{\textwidth}{@{}L{3.3cm}L{2.8cm}Y@{}}
    \toprule
    \textbf{Command} & \textbf{Result} & \textbf{Scope of conclusion} \\
    \midrule
    \code{npm test} & 46/46 test files pass. & Covers backend unit/regression tests and internal integration with mocks or in-memory substitutes; live PostgreSQL and Redis are not confirmed. \\
    \code{npm run test:ai} & 31/31 local-parser cases pass. & Confirms the designed fixture sentences only; it is not out-of-sample accuracy and does not evaluate Gemini, OCR or STT. \\
    \makecell[l]{\code{npm run}\\\code{test:}\\\code{algorithm}} & 8/8 algorithm cases pass. & Checks OLS, anomaly, runway, correlation, aggregation and invariant behavior on controlled data. \\
    \makecell[l]{\code{npm run}\\\code{benchmark:}\\\code{classification}} & 5,328 rows; joint accuracy 26.16\%, macro-F1 0.1565. & Evaluates the local parser on one demo-profile CSV snapshot; it is not production accuracy or an independent test set. \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Algorithm correctness testing}

Table~\ref{tab:algorithm-correctness-cases} lists cases with explicit inputs and expected results. The runner writes JSON/Markdown evidence to \path{demo/evaluation/algorithm/}; the current run passes 8/8 cases.

The two invariant groups use 1,000 seeded inputs to test properties rather than a single example.

\begin{longtable}{@{}L{1.5cm}L{2.8cm}L{4.4cm}L{3.6cm}C{1.4cm}@{}}
  \caption{Algorithm correctness test cases}\label{tab:algorithm-correctness-cases}\\
  \toprule
  \textbf{ID} & \textbf{Objective} & \textbf{Input and expectation} & \textbf{Observed result} & \textbf{Status} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{ID} & \textbf{Objective} & \textbf{Input and expectation} & \textbf{Observed result} & \textbf{Status} \\
  \midrule
  \endhead
  \code{ALG-01} & OLS trend & \code{[100,200,300,400]}; slope=100, $R^2=1$, forecast=500. & Slope, $R^2$ and forecast match. & Pass \\
  \code{ALG-02} & Upper anomaly & Five points, final point 100; only the final point is flagged. & Only \code{d5} is flagged. & Pass \\
  \code{ALG-03} & Sparse boundary & One trend point and three anomaly points; neutral result expected. & First trend value retained; anomaly list empty. & Pass \\
  \code{ALG-04} & Runway & Balance 700,000; one spend day of 700,000 and six zero-spend days. & Average burn 100,000; 7 days left; depletion 2026-07-26. & Pass \\
  \code{ALG-05} & Pearson with joint zeros & Two six-element series; exclude two joint-zero pairs. & $r=-1$, 4 effective pairs, 2 excluded pairs. & Pass \\
  \code{ALG-06} & Monthly aggregation & Three out-of-order rows; preserve empty month and order invariance. & Both input orders produce the same series; February is 100/50. & Pass \\
  \code{ALG-07} & Budget invariant & 1,000 cases, seed 20260814; rounded total never exceeds cap. & 0 violations; maximum over-cap is 0. & Pass \\
  \code{ALG-08} & Goal invariant & 1,000 cases, seed 20260815; higher contribution never lengthens plan. & 0 violations. & Pass \\
  \bottomrule
\end{longtable}

\subsection{Performance evaluation}

The benchmark uses seeded synthetic data at sizes 100, 1,000, 5,000 and 10,000, with five warm-up runs and 30 measured runs per point. Only pure-function time is measured; I/O, PostgreSQL, Redis, workers and network are excluded. The theoretical expectations are $O(n)$ for \code{linearTrend}, $O(n\log n)$ for \code{detectAnomalies} because of sorting, and $O(n+w)$ for \code{completeMonthlyCashflow}, where $w$ is the calendar window.

\begin{longtable}{@{}L{5.2cm}R{1.3cm}L{2.3cm}R{2.0cm}R{2.0cm}@{}}
  \caption{Algorithm runtime benchmark}\label{tab:algorithm-performance}\\
  \toprule
  \textbf{Algorithm} & \textbf{$n$} & \textbf{Expected complexity} & \textbf{Median (ms)} & \textbf{p95 (ms)} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Algorithm} & \textbf{$n$} & \textbf{Expected complexity} & \textbf{Median (ms)} & \textbf{p95 (ms)} \\
  \midrule
  \endhead
  \code{linearTrend} & 100 & $O(n)$ & 0.084 & 0.296 \\
  \code{linearTrend} & 1,000 & $O(n)$ & 0.055 & 0.387 \\
  \code{linearTrend} & 5,000 & $O(n)$ & 0.174 & 1.827 \\
  \code{linearTrend} & 10,000 & $O(n)$ & 0.265 & 0.515 \\
  \code{detectAnomalies} & 100 & $O(n\log n)$ & 0.028 & 0.075 \\
  \code{detectAnomalies} & 1,000 & $O(n\log n)$ & 0.312 & 1.459 \\
  \code{detectAnomalies} & 5,000 & $O(n\log n)$ & 1.654 & 2.417 \\
  \code{detectAnomalies} & 10,000 & $O(n\log n)$ & 3.600 & 4.197 \\
  \makecell[l]{\code{completeMonthly}\\\code{Cashflow}} & 100 & $O(n+w)$ & 0.063 & 0.155 \\
  \makecell[l]{\code{completeMonthly}\\\code{Cashflow}} & 1,000 & $O(n+w)$ & 0.113 & 0.197 \\
  \makecell[l]{\code{completeMonthly}\\\code{Cashflow}} & 5,000 & $O(n+w)$ & 0.250 & 1.423 \\
  \makecell[l]{\code{completeMonthly}\\\code{Cashflow}} & 10,000 & $O(n+w)$ & 0.512 & 0.956 \\
  \bottomrule
\end{longtable}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=2.25cm,y=0.78cm]
    \draw[->] (0,0) -- (4.05,0) node[right] {\small $n$};
    \draw[->] (0,0) -- (0,5.45) node[above] {\small ms};
    \foreach \y in {0,1,2,3,4,5} {\draw (-0.06,\y) -- (0.06,\y) node[left=0.12cm] {\scriptsize \y};}
    \foreach \x/\label in {0/100,1/1k,2/5k,3/10k} {\draw (\x,0.06) -- (\x,-0.06) node[below=0.12cm] {\scriptsize \label};}
    \draw[PerfinBlue, thick] plot coordinates {(0,0.075) (1,1.459) (2,2.417) (3,4.197)};
    \foreach \x/\y in {0/0.075,1/1.459,2/2.417,3/4.197} {\fill[PerfinBlue] (\x,\y) circle (1.4pt) node[above right] {\scriptsize \y};}
  \end{tikzpicture}
  \caption{P95 runtime of \code{detectAnomalies} by input size.}
  \label{fig:algorithm-performance-p95}
\end{figure}

The measurements show increasing runtime with input size in the fixed local environment, most clearly for \code{detectAnomalies}. This is a relative trend comparison for pure functions, not a production latency commitment.
\begin{longtable}{@{}L{1.5cm}L{4.6cm}L{6.5cm}C{1.5cm}@{}}
  \caption{Manual functional test cases}\label{tab:manual-test-cases}\\
  \toprule
  \textbf{ID} & \textbf{Scenario/action} & \textbf{Expected result} & \textbf{Status} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{ID} & \textbf{Scenario/action} & \textbf{Expected result} & \textbf{Status} \\
  \midrule
  \endhead
  \code{TC-M01} & Create, edit and delete a transaction through the form. & The transaction list and wallet balance reflect each operation correctly. & \textbf{Pass} \\
  \code{TC-M02} & Enter a transaction through chat, review its preview and confirm it. & Exactly one record is created, and only after confirmation. & \textbf{Pass} \\
  \code{TC-M03} & Enter incomplete or ambiguous content, then cancel. & The system requests missing information and writes no transaction. & \textbf{Pass} \\
  \code{TC-M04} & Open Dashboard/Report and reconcile totals with the demo data. & Displayed totals match the input data. & \textbf{Pass} \\
  \code{TC-M05} & Export CSV from the interface and open the resulting file. & The file downloads successfully and its figures match the demo data. & \textbf{Pass} \\
  \bottomrule
\end{longtable}

The automated results apply only to the source snapshot, fixtures and configuration used in this run. Tests using mocks or in-memory substitutes do not establish live database, queue or worker behavior. The five manual cases cover selected flows only; they do not support claims about usability, media accuracy, latency or production load.
